### How to Install

```
# back up old .vimrc
cp ~/.vimrc ~/.vimrc.bak.$(date +%s)
cp -r ~/.vim ~/.vim.bak.$(date +%s)

# install my vim
git clone https://github.com/heloowird/vim_setting.git ~ 

# install vundle
git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim
```
